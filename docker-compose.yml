services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arosoft_portfolio
    ports:
      - '9000:9000'
    restart: always
    environment:
      NODE_ENV: production
      PORT: 9000
      HOSTNAME: 0.0.0.0
